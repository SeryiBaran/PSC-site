import ru from './ru.json'
import en from './en.json'

export const messages = {
  ru,
  en,
}

export const config = {
  legacy: false,
  locale: 'ru',
  fallbackLocale: 'en',
  messages,
}

export default messages
